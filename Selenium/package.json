{
  "name": "qajourney-selenium",
  "version": "3.0.0",
  "description": "Selenium WebDriver automation scripts for QAJourney Playground v3",
  "scripts": {
    "test": "npx mocha tests/**/*.test.js --timeout 20000",
    "test:login": "npx mocha tests/login.test.js --timeout 20000"
  },
  "dependencies": {
    "selenium-webdriver": "^4.19.0"
  },
  "devDependencies": {
    "mocha": "^10.4.0"
  }
}
